[package]
name = "capsulet-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Pure decision core for the durable graph runtime: run events, state folding, and what may happen next."

[lib]
path = "src/lib.rs"

[dependencies]
capsulet-ir = { path = "../ir" }
serde.workspace = true
thiserror.workspace = true

[lints]
workspace = true
